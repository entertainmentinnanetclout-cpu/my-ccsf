alter table public.incidents add column if not exists submitted_by uuid references auth.users(id) on delete set null; update public.incidents set submitted_by = reporter_id where submitted_by is null and reporter_id is not null; alter table public.incidents alter column submitted_by set default auth.uid(); create index if not exists idx_incidents_submitted_by on public.incidents(submitted_by);